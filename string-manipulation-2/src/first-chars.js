/* exported firstChars */
/* utilize the substring method and enter the arguments for 0 for starting index, and the length parameter for ending index. */

function firstChars(length, string) {
  return string.substring(0, length);
}
